options:
  type: object
  description: Containing an array of available options matching the query
  properties:
    leg:
      $ref: './options-leg.yaml'
    meta:
      $ref: './type-of-asset.yaml'
